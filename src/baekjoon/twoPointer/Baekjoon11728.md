# 📂 [Algo] 백준 11728 - 배열 합치기

> 날짜: 2026-01-28 | 난이도: Silver 5

### 시간 복잡도
- 배열 2개를 합침 + 정렬 : O(N+M) + O(N+M log(N+M)) // 시간 내에 수행은 가능.
- 투포인터 : O(N log N) + O(M log M) + O(N + M)  // 배열 2개 정렬, 출력은 포인터로

### 알고리즘
- 투포인터 ( 서로 다른 배열의 두 포인터 )

### 핵심 발상
- 각각의 배열을 정렬한다.
- 두 배열의 첫 요소에 포인터를 각각 위치시킨다.
- 두 포인터가 가리키는 숫자를 비교한다.
- 두 포인터가 가리키는 숫자가 같으면 둘 다 출력하고 두 포인터를 둘 다 늘린다.
- 두 포인터가 가리키는 숫자가 작으면 작은 숫자를 출력하고, 작은 쪽 포인터를 늘린다.

### 코드 로직
- 배열 A와 배열 B를 각각 입력받는다.
- A와 B를 먼저 정렬한다.
- A의 포인터와 B의 포인터를 생성하고 두 배열의 맨 앞에 가져다둔다.
- 가리키는 숫자가 더 작은 쪽을 출력 후 포인터를 증가시킨다.
- 같으면 둘 다 출력 후 두 포인터를 증가시킨다.
- 이런 과정을 포인터가 양쪽 끝을 벗어나면 종료한다. ( 출력 후 포인터 이동 )

### 주의점
- 두 배열 중 한 배열이 출력이 완료되도, 다른 하나의 배열에 남은 요소도 처리해야한다.

### 트러블슈팅 & 배운점
- for를 쓰는 투포인터 : 루프가 돌 때마다 인덱스가 무조건 1씩 올라간다.
- while문을 쓰는 투포인터 : 조건에 따라 p1, p2 중 무엇이 올라갈 지 알 수 없다.
- 같은 경우에는 어떤 걸 먼저 출력하든 상관없다. ( 코드 단순화가 가능함 )
- 방금 출력된 값 <= 다음 후보군 중 최솟값이라는 단조성이 유지된다.