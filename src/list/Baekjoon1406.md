# BOJ_1406_에디터

- **알고리즘** : 연결리스트 활용    
- **시간 복잡도** : O(N + M)
- **난이도** : Silver2

## 핵심
- 커서를 기준으로 리스트를 2개로 분리한다.
- O(1)에 삽입,삭제를 위해 연결리스트를 사용했다.

## 주의
- 커서가 문장의 맨 앞이거나 맨 뒤일때를 체크한다. isEmpty() 사용

## 피드백
- Deque 2개로도 구현이 가능하다.
- 단일 리스트에서 인덱스 접근 시 O(N) 시간 초과 방지를 위한 방식 -> 리스트 2개

````
